management:
  endpoints:
    web:
      exposure:
        include: prometheus
      base-path: /
      path-mapping:
        prometheus: /metrics
#  metrics:
#    distribution:
#      percentiles-histogram:
#        http.server.requests: true
#      sla:
#        http.server.requests: 300ms
#  health:
#    rabbit:
#      enabled: false

resilience4j.circuitbreaker:
    backends:
        nambawan:
            ringBufferSizeInClosedState: 5
            ringBufferSizeInHalfOpenState: 3
            waitInterval: 7000
            failureRateThreshold: 40
            ignoreExceptions:
              - com.example.resilience4jkotlin.exception.IgnoreThisException